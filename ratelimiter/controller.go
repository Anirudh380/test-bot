package ratelimiter

func Controller() {
	// tickerPerSecond := time.NewTicker(time.Second / 25)           // 25 requests/second
	// tickerPerMinute := time.NewTicker(time.Minute / 250)          // 250 requests/minute
	// tickerPer30Minutes := time.NewTicker(30 * time.Minute / 1000) // 1000 requests/30 minutes

	// defer tickerPerSecond.Stop()
	// defer tickerPerMinute.Stop()
	// defer tickerPer30Minutes.Stop()

	// ReqPerSec := make(chan int, 25)
	// ReqPerMin := make(chan int, 250)
	// ReqPerHr := make(chan int, 1000)


}
